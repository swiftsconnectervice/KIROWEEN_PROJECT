# Nixpacks configuration for Railway
# Forces Node 20 for Vite 7 compatibility

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
    "npx prisma generate",
    "npm run build"
]

[start]
cmd = "npx ts-node server.ts"
